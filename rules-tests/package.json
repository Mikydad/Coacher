{
  "name": "coach-for-life-rules-tests",
  "description": "Firestore/Storage security-rules tests (run against the emulator)",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:firestore && npm run test:storage",
    "test:firestore": "firebase emulators:exec --only firestore --project demo-stakes-rules 'node --test circles.rules.test.js stakes.rules.test.js user_tree.rules.test.js'",
    "test:storage": "firebase emulators:exec --only firestore,storage --project demo-storage-rules 'node --test storage.rules.test.js'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0"
  }
}
